# 脱敏示例数据

示例数据由项目根目录的 `seed_demo_data.py` 生成，包含三个角色、一个费用报销单、一个两节点顺序审批流程、分析任务、风险发现、报告、审核会话和审计记录。

写入命令：

```bash
uv run python seed_demo_data.py
```

或通过 Docker：

```bash
./交付物/数据库初始化.sh --docker --seed
```

脚本使用固定 UUID、固定文档号 `DEMO-20260818-001` 和冲突忽略策略，重复执行不会创建重复演示事实。演示数据只允许用于开发、联调和验收环境。
