# 系统架构与核心流程图

> 使用 `diagram-builder` 技能生成 Mermaid 图表。
>
> 本文件需要整体审核；标记“【重点审核】”的图表优先检查。

## 1. 系统总体架构图

【重点审核 R-15、R-16、R-17：技术栈、MinIO、OCR/模型适配层及数据出域边界。】

```mermaid
flowchart TB
    U[申请人/审批人员/财务人员/管理员] --> WEB[Vue 3 + TypeScript Web]
    WEB --> API[FastAPI Router]

    subgraph MOD[模块化单体业务模块]
        AUTH[认证与权限]
        DOC[单据与版本]
        FILE[附件与解析]
        SESSION[会话与分析任务]
        RISK[确定性风险规则与人工复核]
        APPROVAL[审批服务/状态机]
        REPORT[报告与审计]
    end
    API --> AUTH & DOC & FILE & SESSION & RISK & APPROVAL & REPORT
    MOD --> UOW[Service / Unit of Work]
    UOW --> REPO[Repository]
    REPO --> PG[(PostgreSQL)]
    UOW --> PROVIDER[ProviderRegistry / Provider Adapter]
    PROVIDER --> HTTPX[httpx：OpenAI-compatible/OCR/RAG 出域]
    UOW --> ACTION[ActionRegistry]
    ACTION --> TOOLS[白名单工具]
    APPROVAL --> STATE[Approval Service / State Machine]
    STATE -.->|审批状态只能由此改变| DOC

    FILE --> STORAGE[FileStorage 接口]
    STORAGE --> LOCAL[(开发：LocalFileStorage)]
    STORAGE --> MINIO[(生产：MinIO Adapter)]

    API --> REDIS[(Redis：队列/缓存/短期撤销键)]
    REDIS --> CELERY[Celery Worker]
    CELERY --> OCR[OCR 任务]
    CELERY --> LLM[LLM 任务]
    CELERY --> RAG[RAG 检索任务]
    CELERY --> REPORT_TASK[Report 任务]
    OCR & LLM & RAG & REPORT_TASK --> AUDIT[调用审计/事实源与证据记录]
    AUDIT --> PG
```

### 调用边界说明

- `Settings` 是配置唯一入口；Router 不直接读取环境变量。
- `AsyncSession` 由请求依赖创建，Service 通过 `Unit of Work` 管理事务，Repository 负责持久化。
- `ProviderRegistry` 选择 OCR、LLM 和 RAG Provider；Provider Adapter 只能通过 `httpx` 调用 OpenAI-compatible 或其他受控服务，必须释放响应和连接。
- `ActionRegistry` 只暴露白名单工具，并在执行前完成权限校验；Agent 不得直接调用未注册工具。
- PostgreSQL 中的单据版本、风险、审批和报告是业务事实源；会话、Redis 和模型输出不能替代事实源。
- 审批状态只能由 `Approval Service / State Machine` 改变，Agent、Provider 和普通 Service 不得直接改写审批状态。

## 2. 费用报销单样板链路

【重点审核 R-01、R-02、R-04、R-08、R-11、R-12：样板范围、人工审批、退回重提、证据、重试和版本绑定。】

```mermaid
flowchart LR
    A([创建费用报销单]) --> B[填写明细与金额]
    B --> C[上传 PDF/PNG/JPG 附件]
    C --> D{格式/权限校验}
    D -- 不通过 --> C
    D -- 通过 --> E[提交并生成不可变版本]
    E --> F[异步 OCR/字段提取]
    F --> G{解析成功?}
    G -- 否 --> H[记录错误并从失败节点重试]
    H --> F
    G -- 是 --> I[规则引擎执行十类风险检查]
    I --> J[绑定证据并生成风险项]
    J --> K[人工复核风险项]
    K --> L{审批人员决定}
    L -- 退回 --> M[修改后重新提交并生成新版本]
    M --> E
    L -- 驳回 --> N([驳回])
    L -- 通过 --> O[生成版本绑定报告]
    O --> P([报告导出/审计追溯])
```

## 3. 单据状态机

【重点审核 R-04、R-05：退回后从首节点重新开始，MVP 仅顺序审批。】

```mermaid
stateDiagram-v2
    [*] --> 草稿
    草稿 --> 已提交 : 提交
    已提交 --> 解析中 : 创建版本/投递任务
    解析中 --> 待复核 : 解析成功
    解析中 --> 解析失败 : 解析异常
    解析失败 --> 解析中 : 有权限用户重试
    待复核 --> 审批中 : 风险复核完成
    审批中 --> 审批中 : 顺序节点通过
    审批中 --> 退回 : 审批人员退回
    退回 --> 已提交 : 修改后重新提交/新版本
    审批中 --> 驳回 : 审批人员驳回
    审批中 --> 已通过 : 最终节点通过
    草稿 --> 已作废 : 作废
    退回 --> 已作废 : 作废
```

## 4. 核心数据关系图

【重点审核 R-03、R-06、R-08、R-12：数据权限、金额精度、证据链和版本绑定。】

```mermaid
erDiagram
    USER ||--o{ DOCUMENT : 创建
    USER ||--o{ AUDIT_LOG : 操作
    DOCUMENT ||--o{ DOCUMENT_VERSION : 生成
    DOCUMENT_VERSION ||--o{ DOCUMENT_ITEM : 包含
    DOCUMENT_VERSION ||--o{ ATTACHMENT : 关联
    ATTACHMENT ||--o{ PARSE_RESULT : 产生
    DOCUMENT_VERSION ||--o{ RISK_ANALYSIS : 执行
    RISK_ANALYSIS ||--o{ RISK_ITEM : 包含
    RISK_ITEM ||--o{ EVIDENCE : 引用
    DOCUMENT_VERSION ||--o{ APPROVAL_INSTANCE : 发起
    APPROVAL_INSTANCE ||--o{ APPROVAL_TASK : 包含
    DOCUMENT_VERSION ||--o{ REPORT : 生成

    DOCUMENT {
        uuid id PK
        uuid applicant_id FK
        string document_type
        string status
    }
    DOCUMENT_VERSION {
        uuid id PK
        uuid document_id FK
        int version_no
        string currency
        decimal total_amount
        datetime submitted_at
    }
    RISK_ITEM {
        uuid id PK
        uuid analysis_id FK
        string severity
        decimal actual_value
        decimal reference_value
        decimal threshold
        string rule_version
    }
    EVIDENCE {
        uuid id PK
        uuid risk_item_id FK
        uuid attachment_id FK
        int page_no
        string text_snippet
        decimal confidence
    }
```

## 5. 审核顺序

1. 先审核总体架构是否符合模块化单体和既定技术栈。
2. 再审核费用报销单样板链路是否完整闭环。
3. 再审核退回、重试、人工审批和版本绑定。
4. 最后审核数据对象的金额、证据、权限字段。
